/**
 * Phase 6 — Firebase Admin SDK / Firestore singleton init.
 * Wire credentials from env and export `db`.
 */
export function getDb(): never {
  throw new Error('Not implemented: Firestore Admin init (Phase 6)');
}
