rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All app data is accessed via the Admin SDK from apps/api.
    // Deny all client SDK access (Section 10).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
